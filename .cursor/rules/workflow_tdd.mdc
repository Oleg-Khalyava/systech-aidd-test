---
alwaysApply: false
---
# TDD Workflow

> **Test-Driven Development** - сначала тесты, потом код

## Основные правила

### 1. Работа по плану
- Выполнять задачи **строго** по `tasklist.md`
- Следовать итерационному подходу (Итерация 1 → 2 → 3 → 4)
- Не переходить к следующей итерации без завершения текущей
- **Каждая фича начинается с теста!**

### 2. TDD цикл разработки каждой задачи

#### Этап 1: Согласование
- Изучить задачу из tasklist
- Предложить решение с конкретными фрагментами кода
- Описать какие файлы будут созданы/изменены
- **Определить тестовые сценарии** для фичи
- **Ожидать подтверждения** от разработчика

#### Этап 2: Реализация (TDD цикл)

##### 🔴 RED - Пиши тест, который падает
1. **Создай тест для новой фичи**
   ```python
   # tests/test_new_feature.py
   def test_new_feature_success():
       """Тест новой фичи"""
       # Arrange
       component = Component()

       # Act
       result = component.new_feature(input_data)

       # Assert
       assert result == expected_value
   ```

2. **Запусти тест - убедись, что падает**
   ```bash
   make test  # или pytest tests/test_new_feature.py -v
   ```

3. **Проверь, что тест падает по правильной причине**
   - ❌ `AttributeError: 'Component' has no attribute 'new_feature'`
   - ✅ Тест падает, потому что фича еще не реализована

##### 🟢 GREEN - Напиши минимум кода для прохождения
1. **Реализуй минимальное решение**
   ```python
   # src/component.py
   class Component:
       def new_feature(self, input_data):
           # Минимальная реализация для прохождения теста
           return expected_value
   ```

2. **Запусти тест - убедись, что проходит**
   ```bash
   make test  # или pytest tests/test_new_feature.py -v
   ```

3. **Все тесты должны быть зелеными!**
   - ✅ Новый тест проходит
   - ✅ Все старые тесты тоже проходят

##### 🔵 REFACTOR - Улучши код
1. **Улучши реализацию** (если нужно)
   - Убери дублирование (DRY)
   - Упрости код (KISS)
   - Улучши читаемость
   - Следуй принципам из `vision.md`

2. **Запусти тесты после каждого изменения**
   ```bash
   make test
   ```

3. **Тесты должны оставаться зелеными!**
   - ✅ Рефакторинг не ломает функционал

##### 🔁 REPEAT - Следующий тест
- Вернись к шагу 🔴 RED для следующей части фичи
- Продолжай пока фича не будет полностью реализована

##### ✅ VERIFY - Проверка качества
```bash
make format      # Форматирование
make lint        # Линтер
make type-check  # Проверка типов
make test-cov    # Coverage проверка
```

#### Этап 3: Подтверждение
- После реализации **ожидать подтверждения** работы
- Показать результаты тестов и coverage
- Исправить замечания при необходимости
- **Все тесты зеленые!**

#### Этап 4: Обновление прогресса
- Обновить статус задачи в `tasklist.md`:
  - ⏳ Не начато → 🔄 В работе → ✅ Завершено
- Отметить выполненные пункты чекбоксами `[x]`
- Обновить таблицу прогресса с датами

---

## TDD Чеклист для каждой задачи

### Подготовка
- [ ] Прочитать задачу из tasklist
- [ ] Предложить решение с кодом
- [ ] Определить тестовые сценарии
- [ ] Получить подтверждение

### TDD цикл
- [ ] 🔴 **RED**: Написать падающий тест
- [ ] 🔴 **RED**: Запустить тест, убедиться что падает
- [ ] 🟢 **GREEN**: Написать минимум кода для прохождения
- [ ] 🟢 **GREEN**: Запустить тест, убедиться что проходит
- [ ] 🔵 **REFACTOR**: Улучшить код (если нужно)
- [ ] 🔵 **REFACTOR**: Запустить тесты, убедиться что зеленые
- [ ] 🔁 **REPEAT**: Повторить для следующей части фичи
- [ ] ✅ **VERIFY**: Запустить все проверки качества

### Завершение
- [ ] Получить подтверждение работы
- [ ] Проверить coverage (≥80%)
- [ ] Обновить tasklist (статусы, чекбоксы, даты)
- [ ] Согласовать переход к следующей задаче

---

## Команды для TDD

### Быстрый цикл разработки
```bash
# Запуск конкретного теста (быстро)
pytest tests/test_new_feature.py -v

# Запуск с автоперезапуском при изменениях
pytest tests/test_new_feature.py -v --watch  # требует pytest-watch

# Остановка на первой ошибке
pytest tests/test_new_feature.py -x

# Запуск только провалившихся тестов
pytest tests/ --lf
```

### Полная проверка
```bash
# Все тесты
make test

# Тесты с coverage
make test-cov

# Полная проверка качества
make check  # format + lint + type-check + test
```

### Мониторинг coverage
```bash
# Отчет в консоли
coverage report

# HTML отчет
coverage html && start htmlcov/index.html  # Windows
coverage html && open htmlcov/index.html   # MacOS/Linux
```

---

## Примеры TDD сценариев

### Новая фича: Rate Limiting

#### 🔴 RED - Тест 1: Первый запрос разрешен
```python
# tests/test_rate_limit.py
def test_first_request_allowed():
    """TDD RED: первый запрос должен быть разрешен"""
    middleware = RateLimitMiddleware(rate_limit=2)

    result = middleware.check(user_id=123)

    assert result is True
```

```bash
$ make test
# ❌ FAILED: AttributeError: 'RateLimitMiddleware' has no attribute 'check'
```

#### 🟢 GREEN - Минимальная реализация
```python
# src/middlewares/rate_limit.py
class RateLimitMiddleware:
    def __init__(self, rate_limit: int):
        self.rate_limit = rate_limit

    def check(self, user_id: int) -> bool:
        return True  # Минимум для прохождения теста
```

```bash
$ make test
# ✅ PASSED: test_first_request_allowed
```

#### 🔴 RED - Тест 2: Быстрые запросы блокируются
```python
def test_fast_requests_blocked():
    """TDD RED: быстрые запросы должны блокироваться"""
    middleware = RateLimitMiddleware(rate_limit=2)

    middleware.check(123)  # Первый запрос
    result = middleware.check(123)  # Второй сразу

    assert result is False
```

```bash
$ make test
# ❌ FAILED: assert True is False (check всегда возвращает True)
```

#### 🟢 GREEN - Полная реализация
```python
from time import time

class RateLimitMiddleware:
    def __init__(self, rate_limit: int):
        self.rate_limit = rate_limit
        self.last_request: dict[int, float] = {}

    def check(self, user_id: int) -> bool:
        now = time()
        last = self.last_request.get(user_id, 0)

        if now - last < self.rate_limit:
            return False

        self.last_request[user_id] = now
        return True
```

```bash
$ make test
# ✅ PASSED: test_first_request_allowed
# ✅ PASSED: test_fast_requests_blocked
```

#### 🔵 REFACTOR - Улучшение
```python
# Добавляем type hints, docstrings, константы
from time import time
from typing import Dict

class RateLimitMiddleware:
    """Middleware для ограничения частоты запросов"""

    def __init__(self, rate_limit: int = 2):
        """
        Args:
            rate_limit: Минимальный интервал между запросами (секунды)
        """
        self._rate_limit = rate_limit
        self._last_request: Dict[int, float] = {}

    def check(self, user_id: int) -> bool:
        """Проверяет, можно ли обработать запрос пользователя

        Args:
            user_id: ID пользователя

        Returns:
            True если запрос разрешен, False если нужно подождать
        """
        now = time()
        last = self._last_request.get(user_id, 0)

        if now - last < self._rate_limit:
            return False

        self._last_request[user_id] = now
        return True
```

```bash
$ make test
# ✅ Все тесты зеленые

$ make check
# ✅ format: OK
# ✅ lint: OK
# ✅ type-check: OK
# ✅ test: OK
```

---

## Переход к следующей итерации

- **Согласовать** переход к следующей итерации
- Убедиться, что текущая итерация полностью завершена
- **Все тесты зеленые!**
- Coverage ≥ 80% (проверить `make test-cov`)
- Код прошел все проверки качества (`make check`)

---

## Коммиты

### После каждой фичи (опционально)
```bash
git add .
git commit -m "test: add rate limiting tests"
git commit -m "feat: implement rate limiting"
```

### После завершения итерации (обязательно)
```bash
git add .
git commit -m "feat: iteration N - краткое описание + tests"
```

**Формат сообщения:**
- `feat: iteration N - description + tests (coverage: XX%)`
- Пример: `feat: iteration 1 - infrastructure and basic bot + tests (coverage: 85%)`

---

## Золотые правила TDD

### ✅ Делай
1. **Пиши тест первым** - всегда!
2. **Пиши минимум кода** для прохождения теста
3. **Рефактори смело** - тесты защищают от регрессии
4. **Запускай тесты часто** - после каждого изменения
5. **Один тест = одна проверка** - простота и ясность
6. **Commit часто** - маленькие шаги

### ❌ Не делай
1. **Не пиши код без теста** - это не TDD!
2. **Не пропускай RED фазу** - убедись что тест падает
3. **Не пиши сразу "правильный" код** - минимум сначала
4. **Не оставляй падающие тесты** - фикси сразу
5. **Не тестируй boilerplate** - фокус на логике
6. **Не гонись за 100% coverage** - качество важнее

---

## Мантра TDD

> **RED → GREEN → REFACTOR → REPEAT**
>
> 1. 🔴 Пиши тест, который падает
> 2. 🟢 Пиши код, который проходит
> 3. 🔵 Улучши код
> 4. 🔁 Повторяй

**Помни:** Если тест не падал - ты не знаешь, что он проверяет!

---

## Референс

- Roadmap проекта: [roadmap.md](../../docs/roadmap.md)
- QA соглашения: [qa_conventions.mdc](./qa_conventions.mdc)
- Общие соглашения: [conventions.mdc](./conventions.mdc)
- Базовый workflow: [workflow.mdc](./workflow.mdc)
- Архитектура: [vision.md](../../docs/vision.md)
